kay, let's talk through this point by point. I'm reading the todo.md file, and I wanna respond to it so that we don't miss anything critical. Philosophy, command hierarchy in priority order. Specific command should always take precedent over the generic other commands. And they should exist when there is domain knowledge that needs to be either conveyed or there are multiple records that need to be shown connected that don't always make sense.

If there is no domain specific command, then the records command should be next, which is generic create, read, update, delete on any table.

If the records command doesn't meet the need that is there, then we should fall back onto the API, the rest command, which really is just a wrapper for generic APIs. ServiceNow has a couple of ones that I should name out here, like the table API, which is where most of the records commands come from. The stats API, which is an anchor count for the APIs, but there's a number of other commands as well.

If the top three things above us do not meet the need of whatever is being asked for, we should not create Python scripts or Node.js scripts or Go scripts. We should at that point say that there isn't anything developed for that and we should log in issue.or ask the human for some more information about how they can do this.or search the docs or... Yeah, that's it.

All right, let's get into the commands. You have listed in the core keep as is the records command, which is the workhorse generic crud on any table. The rest command you have is keep because this is an escape hatch. Raw rest calls for anything the CLI doesn't cover. And then next we have the tables command, which allows for schema introspection, inheritance trees, column definitions, allows the creation of tables. And I guess technically you could get this with records. But they're different enough. I'd rather keep that separate.

The eval command I added to allow writing for background scripts, which is super useful. So we'll keep that. The auth config and setup are all sort of intertwined. They allow for managing your profiles or authenticating or setting up this client. And maybe we can wrap that into a bow and make it a better command. I don't know. Yeah. Well, I'll leave that there. And then the docs command, which is documentation that we fetch from a website that I managed called sn.jsonpro. And then there's a commands command, which is a command catalog, which I don't know that we'll need if we update the commands to be discoverable. And then there's a command for version, which I'm not sure we also need. I don't know if there should be a command for that or if it should just be like an optional parameter passed.

All right, then we have the session state commands. One for update set and one for scope. And we should probably keep them both. Because you need to be in a specific update set when you make changes or specific scopes, you make those changes.

Okay, let's get into the domain-specific commands that have some nuance to them. The biggest of which is the flows command, where you're suggesting to drop the list, keep the show, keep the executions, keep the execute, which I would actually agree with. Some of this. I do like the list part of the commands though, because I like to be able to search for the commands based on name, if I ask for, hey, there's a flow that's called, I don't know, emergency change. Can you find any of the flows attached to that? And it does a search, and maybe the flow is called standard emergency change or something. But I got the name wrong. How would we find that otherwise?

<Okay, let's talk about the jobs command. Your suggestions to drop the list. Most of these domain-specific commands you've suggested to drop the list. So let's talk about why I want to keep a list-like thing or like a search-like thing. I don't know the names of some of these records, and I like to be able to search for them, and if I'm only going to show one that leaves, I think, a lot to be, that leaves a gap. So I'd like to... How about this? Come up with some alternative to dropping list and dropping show and making it so that we can query the records and pick one.

All right, beyond the list subcommand of jobs, you also said to drop the logs and drop script. What are logs and script even do on the jobs? I'm sure we'll keep on keeping executions. I like that things are keeping. I'm just not sure about the logs and script.

All right, for the command rules, you're seeing the drop the list, keep show, and then keep the script, which is just a script field, useful for piping. I don't know how to add to that. Script includes drop the list, again, let's go back to the whole list discussion, keep show, code, extract, skip for piping. In addition, I want for script includes to show the common patterns that ServiceNow uses for script includes. And there's not that many of them, but there's like just a function, there's class-based, there's extended class, and I think that's it.

Regarding client scripts. Keep show, Curie View highlights on the on-chain, on submit, on load, on cell edit, table context, and then the script itself. Sure. But also, client scripts could include catalog item client scripts. I don't know if we want to have a sub-command within the catalog item itself, But we may want to have that there.

U.I. Scripts, I don't know if I even add to that conversation. ACLs. I want to add more stuff to ACLs, but I haven't written a downer set it. Access controls are complicated in ServiceNow, but I want its own place for it, because security or access control is just interesting in ServiceNow.

All right, UI policies should be used in favor over client scripts when possible. However, you shouldn't script on UI policies because not many people look there. UI policies and data policies are also very similar except data policies can be enforced anywhere and on the server side, whereas UI policies is a client side enforcement. UI policies have a related list of records saying that this field is either read only, hidden or mandatory or a combination thereof, based on conditions on the UI policy record itself, which is why I want to have its own namespace here.

Choices in ServiceNow are weird. There's a table in ServiceNow that controls what's in most drop downs called cis choice. And they're inherited in weird ways. And they're even tracked in weird ways and how it's captured and updates that. Choices can be inherited down meaning like a choice for task.state will be inherited to incident.state. Unless incident has its own choices on incident.state. And that's because incident is a child of task.

Next up we have forms. I want to be able to display the form layouts in ServiceNow because it's hard to visualize it in code when you're reviewing updates that's or reviewing some of these work unless you look at the forms in the system. And I want to be able to ask the AI to add a field to a form or remove a field from a form or from a section. And it know what that means.

All right, so then we have the command for lists. Lists are also weird. Lists like forms are controlled by either the sysUI list table with the sysUI list lintable. It can also be controlled. There's another space for workspaces where the lists are maintained, which are different, which I haven't documented written down yet.

Logs. So logs are super useful because going to the syslog table can be a huge pin in the next since there's so many logs in most instances and being able to search them is fantastic.

catalog item. I want to keep this in its own namespace because creating catalog items is just it's a bear you have to create an sccat item that sccat item has to have related sc item options which are actually not stored like they don't have a reference directly to the sccat item there's another table in the middle which is that sc item option m-tomb but then there's also variable sets which aren't necessarily attached to a single item with an sc item option m-tomb record it's yet another record on top of that um we have the client scripts that i mentioned before which can also apply differently to these caligatums there's an extension from client scripts for caliglion scripts that and then the variables their types are just unique they're different they're not the same as the dictionary elements for the types and they have their own for each type of variable they have their own unique requirements for instance like a reference requires a specific table field being set on the record and a condition field being set on the record to limit those choices

variable and variable types. This is more for me just trying to show like the data that's required for the catalog stuff. So maybe there's a way we can include that data when creating these things or abstract it so that it's not a pain in the butt because most of the time you're just using single line text, multi line text, reference, choice, multi choice or drop down.


Okay, let's talk about the service portal components. The service portal portals, the service portal widgets, and the service portal pages. Actually, the hierarchy is service portals, service portal pages, and service portal widgets. Pages aren't locked to a specific portal, so I actually wouldn't be opposed to pulling portals out. That being said, widgets require Angular 153 code to work on the client side. And, yeah, they're just different. There's four script fields on a widget. There's an HTML template field, there's a server-side script field, there's a client-side script field, and there's a CSS field. In addition, there's an option schema which can be used to control how these things work. There's also a widget instance which is used to connect a widget to a page. That's a great segue for the pages. Pages control what SP rectangles and containers exist, and those things are related to the widget instances which I'll say like in this 12 wide container, there'll be three, four wide blocks that have widget instances for an incident list, a change list, and a request list. Those connections are just interesting, so it'd be nice actually to be able to render that out into the terminal when Aaz asking about what is on a page. But also, it'd be nice to be able to describe what you want in text and have it make it for you.

Alright, the kill entirely kill compare kill export kill import kill generate I agree. Let's just get rid of those

All right, so outstanding just to reiterate Can you come up with a different way so we can? query and find a specific item in a way that makes sense that we can both show sort of an interactive list a Static list and then picking a record from that list

